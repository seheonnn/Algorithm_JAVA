- 주문량이 많은 아이스크림들 조회하기

```
SELECT F.FLAVOR FROM FIRST_HALF F
JOIN (SELECT SHIPMENT_ID, FLAVOR, SUM(TOTAL_ORDER) AS TOTAL_ORDER FROM JULY GROUP BY FLAVOR) J
ON F.SHIPMENT_ID = J.SHIPMENT_ID
ORDER BY (F.TOTAL_ORDER + J.TOTAL_ORDER) DESC LIMIT 3
```
